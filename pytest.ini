[pytest]
# Scopes discovery to this project's tests. Without it, `pytest -q` from the
# repo root also collects vendor/sigma/tests/ once the Sigma corpus is cloned
# (scripts/refresh_sigma.ps1 / refresh_sigma.sh) -- that suite has its own
# dependencies (colorama) this project does not install.
testpaths = tests
